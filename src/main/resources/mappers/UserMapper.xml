<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.maple.primary.mapper.UserMapper">
    <!-- 新增  -->
    <insert id="insert" parameterType="UserDo" useGeneratedKeys="true" keyProperty="id">
        insert into user(username, password, random, name)
        values (#{username}, #{password}, #{random}, #{name})
    </insert>
    <!--  更新  -->
    <update id="update" parameterType="UserDo">
        update user
        <set>
            <if test="name != null">
                name = #{name},
            </if>
            <if test="username != null">
                username = #{username},
            </if>
            <if test="password != null">
                password = #{password},
            </if>
            <if test="status != null">
                status = #{status},
            </if>
            <if test="deleted != null">
                is_deleted = #{deleted},
            </if>
        </set>
        where id = #{id}
    </update>

    <select id="getById" resultType="UserDO">
        SELECT *
        FROM user
        WHERE id = #{id}
    </select>

    <select id="getByUsername" resultType="UserDO">
        SELECT *
        FROM user
        WHERE username = #{username}
    </select>

    <delete id="delete">
        delete
        from user
        where id = #{id}
    </delete>
</mapper>
